# Student Autocommiter 🚀

**Инструмент для автоматического создания Git коммитов с реалистичной историей (для образовательных целей)**

[![Python Version](https://img.shields.io/badge/python-3.8%2B-blue)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Windows](https://img.shields.io/badge/OS-Windows-blue)](https://www.microsoft.com/windows)
[![Git Required](https://img.shields.io/badge/Git-Required-orange)](https://git-scm.com/)

> ⚠️ **ВАЖНО: ЭТО ИНСТРУМЕНТ ДЛЯ ОБРАЗОВАТЕЛЬНЫХ ЦЕЛЕЙ**  
> Используйте только для изучения Git автоматизации на своих собственных репозиториях.

## 📋 Содержание

- [Особенности](#особенности)
- [Быстрый старт](#быстрый-старт)
- [Установка](#установка)
- [Использование](#использование)
- [Конфигурация](#конфигурация)
- [Архитектура](#архитектура)
- [Компоненты](#компоненты)
- [CI/CD](#cicd)
- [Этические соображения](#этические-соображения)
- [Разработка](#разработка)
- [Лицензия](#лицензия)

## ✨ Особенности

- **🤖 Автоматические коммиты** - Планирование и создание коммитов по расписанию
- **🎭 Реалистичные изменения** - Генерация правдоподобного кода и сообщений коммитов
- **⚙️ Гибкая конфигурация** - Полная настройка через YAML файлы
- **🔄 Обработка ошибок** - Система повторов и очередь отложенных коммитов
- **🚀 Интеграция с ОС** - Автозапуск на Windows через реестр или Планировщик задач
- **📊 Мониторинг** - Статистика, логирование и отчеты
- **🔒 Безопасность** - Проверки и ограничения для ответственного использования

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- Git установленный и доступный в PATH
- Windows (основная ОС), также работает на Linux/macOS

### Установка за 5 минут

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/yourusername/student-autocommiter.git
cd student-autocommiter

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Инициализируйте конфигурацию
python src/main.py init

# 4. Настройте путь к репозиторию
python src/main.py config set repository.local_path "C:/Projects/my-repo"

# 5. Создайте тестовый коммит
python src/main.py test-commit

# 6. Запустите в тестовом режиме
python src/main.py start --test
```

## 📦 Установка

### Полная установка

```bash
# 1. Установите Git (если еще не установлен)
# Windows: https://git-scm.com/download/win
# Linux: sudo apt install git
# macOS: brew install git

# 2. Установите Python зависимости
pip install gitpython pyyaml schedule python-dateutil

# Для Windows автозапуска:
pip install pywin32

# 3. Создайте структуру проекта
mkdir -p student-autocommiter/{src,config,logs,data}
cd student-autocommiter
```

### Проверка установки

```bash
# Проверьте версии
python --version
git --version

# Запустите проверку здоровья
python src/main.py status
```

## 🎮 Использование

### Основные команды

```bash
# Инициализация проекта
python src/main.py init

# Работа с конфигурацией
python src/main.py config show
python src/main.py config set repository.local_path "/path/to/repo"
python src/main.py config get commit_settings.min_commits_per_day

# Запуск автокоммиттера
python src/main.py start                 # Интерактивный режим
python src/main.py start --background    # Фоновый режим
python src/main.py start --test          # Тестовый режим (один коммит)

# Установка автозапуска
python src/main.py install-service      # Автоматический выбор метода
python src/main.py install-service --method registry
python src/main.py install-service --method task_scheduler

# Мониторинг и отладка
python src/main.py status               # Статус репозитория
python src/main.py history --limit 10   # История коммитов
python src/main.py stats                # Статистика работы
python src/main.py test-commit          # Создать тестовый коммит
```

### Пример рабочего процесса

```bash
# 1. Настройте новый проект
python src/main.py init
python src/main.py config set repository.local_path "C:/Projects/learning-git"
python src/main.py config set commit_settings.min_commits_per_day 2
python src/main.py config set commit_settings.max_commits_per_day 5

# 2. Проверьте конфигурацию
python src/main.py config show

# 3. Протестируйте
python src/main.py test-commit
python src/main.py start --test

# 4. Установите автозапуск (опционально)
python src/main.py install-service

# 5. Запустите постоянно
python src/main.py start --background
```

## ⚙️ Конфигурация

### Основные настройки

```yaml
# config/user_config.yaml
repository:
  url: ""                    # URL удаленного репозитория (опционально)
  local_path: "./workspace"  # Путь к локальному репозиторию
  branch: "main"             # Ветка для коммитов
  auto_push: true           # Автоматически пушить после коммита

commit_settings:
  min_commits_per_day: 1    # Минимальное количество коммитов в день
  max_commits_per_day: 8    # Максимальное количество коммитов в день
  active_hours:
    start: 9                # Начало рабочего дня (9:00)
    end: 21                 # Конец рабочего дня (21:00)
  weekend_activity: false   # Создавать коммиты по выходным

auto_start:
  enabled: false           # Включить автозапуск
  startup_method: "registry" # Метод: registry, task_scheduler, service
  delay_minutes: 5         # Задержка после старта системы

error_handling:
  retry_attempts: 3       # Количество попыток повтора
  queue_failed_commits: true # Сохранять неудачные коммиты в очередь
```

### Расширенные настройки

```yaml
stealth_mode:
  enabled: true           # Режим "стелс" для реалистичности
  random_delays: true     # Случайные задержки между коммитами
  vary_commit_times: true # Изменение времени коммитов
  realistic_messages: true # Реалистичные сообщения коммитов
  max_changes_per_commit: 5 # Максимальное количество файлов за коммит

patterns:
  commit_messages_file: "./config/patterns/commit_messages.txt"
  code_snippets_dir: "./config/patterns/code_snippets"
  file_patterns: "./config/patterns/file_patterns.json"

logging:
  level: "INFO"          # DEBUG, INFO, WARNING, ERROR
  file: "./logs/autocommiter.log"
  max_size_mb: 10        # Максимальный размер лог-файла
```

## 🏗️ Архитектура

```
student-autocommiter/
├── src/                    # Исходный код
│   ├── main.py            # Точка входа CLI
│   ├── config_manager.py  # Управление конфигурацией
│   ├── git_operations.py  # Работа с Git
│   ├── commit_generator.py # Генератор коммитов
│   ├── scheduler.py       # Планировщик задач
│   ├── error_handler.py   # Обработка ошибок
│   └── system_integration.py # Интеграция с ОС
├── config/                # Конфигурационные файлы
├── logs/                  # Логи приложения
├── data/                  # Данные (очереди, статистика)
├── tests/                 # Тесты
└── docs/                  # Документация
```

## 🔧 Компоненты

### 1. ConfigManager
Управление YAML конфигурацией с поддержкой:
- Загрузки/сохранения настроек
- Валидации значений
- Слияния default и user конфигов
- Dot-notation для доступа к настройкам

### 2. GitOperations
Работа с Git репозиториями:
- Инициализация/клонирование репозиториев
- Создание коммитов с изменениями
- Push/Pull/Fetch операции
- Получение статуса и истории

### 3. CommitGenerator
Генерация реалистичных коммитов:
- Шаблоны сообщений коммитов
- Сниппеты кода для изменений
- Разные типы изменений (новые файлы, модификации, рефакторинг)
- Паттерны имен файлов

### 4. Scheduler
Планировщик задач:
- Распределение коммитов по времени
- Учет рабочих часов и выходных
- Фоновое выполнение
- Планирование на день вперед

### 5. ErrorHandler
Обработка ошибок:
- Очередь отложенных коммитов
- Повторные попытки
- Статистика ошибок
- Логирование инцидентов

### 6. SystemIntegration
Интеграция с ОС Windows:
- Автозапуск через реестр
- Планировщик задач Windows
- Создание служб (в разработке)

## 🔄 CI/CD

Проект включает GitHub Actions workflow для автоматической проверки:

```yaml
# .github/workflows/test.yml
name: Test and Lint
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m pytest tests/
      - name: Run linter
        run: flake8 src/
```

## ⚖️ Этические соображения

### ✅ Допустимое использование
- Изучение Git автоматизации
- Тестирование CI/CD пайплайнов
- Создание демонстрационных репозиториев
- Образовательные проекты

### ❌ Недопустимое использование
- Обман преподавателей или работодателей
- Фальсификация академической работы
- Введение в заблуждение о реальной активности
- Нарушение политик академической честности

### Предупреждение при запуске
При каждом запуске инструмент показывает этическое предупреждение и требует подтверждения.

## 🛠️ Разработка

### Установка для разработки

```bash
# Клонирование
git clone https://github.com/yourusername/student-autocommiter.git
cd student-autocommiter

# Установка в режиме разработки
pip install -e .

# Установка dev зависимостей
pip install pytest pytest-cov flake8 mypy

# Запуск тестов
pytest tests/ -v --cov=src

# Проверка типов
mypy src/

# Проверка стиля кода
flake8 src/
```

### Структура кода

```python
# Пример структуры модуля
class Component:
    """Краткое описание компонента"""
    
    def __init__(self, config):
        """Инициализация компонента
        
        Args:
            config: Конфигурация компонента
        
        Raises:
            ConfigError: При неверной конфигурации
        """
        self.config = config
    
    def method(self, param):
        """Выполняет действие
        
        Args:
            param: Параметр действия
        
        Returns:
            Результат выполнения
        
        Raises:
            OperationError: При ошибке выполнения
        """
        # Реализация
```

### Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/

# Запуск конкретного теста
python -m pytest tests/test_git_operations.py -v

# Тест с покрытием
python -m pytest tests/ --cov=src --cov-report=html

# Запуск демо
python demo_autocommit.py
```

## 📝 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- Разработчикам GitPython за отличную библиотеку
- Сообществу Python за потрясающие инструменты
- Всем, кто использует инструмент ответственно

---

<div align="center">

**Используйте ответственно и только в образовательных целях!**

[Report Bug](https://github.com/yourusername/student-autocommiter/issues) · 
[Request Feature](https://github.com/yourusername/student-autocommiter/issues) · 
[Documentation](docs/)

</div>