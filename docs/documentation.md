# 📚 Student Autocommiter - Краткая документация

## 📋 Быстрый старт (1 минута)

```bash
# Установка и запуск
git clone <репозиторий>
cd student-autocommiter
pip install -r requirements.txt
python src/main.py init
python src/main.py test-commit
```

## 🎯 Основные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `init` | Инициализация проекта | `python src/main.py init` |
| `start` | Запуск автокоммиттера | `python src/main.py start` |
| `test-commit` | Тестовый коммит | `python src/main.py test-commit` |
| `status` | Статус репозитория | `python src/main.py status` |
| `history` | История коммитов | `python src/main.py history --limit 5` |
| `config show` | Показать конфигурацию | `python src/main.py config show` |
| `config set` | Изменить настройку | `python src/main.py config set repository.local_path "./myrepo"` |

## ⚙️ Конфигурация (обязательные настройки)

```yaml
# config/user_config.yaml
repository:
  local_path: "/путь/к/вашему/репозиторию"  # ⚠️ ОБЯЗАТЕЛЬНО
  branch: "main"

commit_settings:
  min_commits_per_day: 2
  max_commits_per_day: 6
  active_hours:
    start: 9
    end: 21
```

## 🚀 Режимы запуска

### 1. **Тестовый режим** (рекомендуется сначала)
```bash
# Создаст один коммит и завершит работу
python src/main.py test-commit

# Или
python src/main.py start --test
```

### 2. **Интерактивный режим**
```bash
# Запуск с выводом логов в консоль
python src/main.py start
# Ctrl+C для остановки
```

### 3. **Фоновый режим**
```bash
# Работает в фоне
python src/main.py start --background
# Остановка: закрыть терминал или kill процесс
```

### 4. **Автозапуск при старте Windows**
```bash
# Установить автозапуск
python src/main.py install-service

# Удалить автозапуск
python src/main.py uninstall-service
```

## 📁 Структура проекта

```
student-autocommiter/
├── src/                    # Исходный код
│   ├── main.py            # Главный файл (запуск отсюда)
│   ├── config_manager.py  # Конфигурация
│   ├── git_operations.py  # Работа с Git
│   └── ...               # Другие модули
├── config/                # Настройки
│   ├── default_config.yaml
│   └── user_config.yaml   # ⬅️ Ваши настройки здесь
├── logs/                  # Логи
├── data/                  # Данные (очереди, статистика)
└── requirements.txt       # Зависимости
```

## 🔧 Работа с настройками

### Просмотр всех настроек:
```bash
python src/main.py config show
```

### Изменение настроек:
```bash
# Установить путь к репозиторию
python src/main.py config set repository.local_path "C:/Projects/my-project"

# Установить количество коммитов
python src/main.py config set commit_settings.min_commits_per_day 3
python src/main.py config set commit_settings.max_commits_per_day 8

# Рабочие часы
python src/main.py config set commit_settings.active_hours.start 10
python src/main.py config set commit_settings.active_hours.end 20

# Включить автозапуск
python src/main.py config set auto_start.enabled true
```

### Просмотр конкретной настройки:
```bash
python src/main.py config get repository.local_path
```

## 🐛 Решение проблем

### Ошибка: "Cannot choose from an empty sequence"
```bash
# Создайте необходимые файлы
mkdir -p config/patterns
echo "Fix bug" > config/patterns/commit_messages.txt
echo "Add feature" >> config/patterns/commit_messages.txt
```

### Ошибка: "Repository not configured"
```bash
# Настройте путь к репозиторию
python src/main.py config set repository.local_path "/реальный/путь"
```

### Git не установлен
- **Windows**: Скачайте с [git-scm.com](https://git-scm.com/download/win)
- **Linux**: `sudo apt install git`
- **macOS**: `brew install git`

### Ошибки импорта
```bash
# Убедитесь, что вы в директории проекта
cd student-autocommiter

# Или добавьте в PYTHONPATH
export PYTHONPATH=$(pwd):$PYTHONPATH  # Linux/macOS
set PYTHONPATH=%cd%;%PYTHONPATH%      # Windows
```

## 📊 Мониторинг работы

### Статистика:
```bash
python src/main.py stats
```
Вывод:
- Всего коммитов
- Успешные/неудачные
- В очереди
- Процент успеха

### Логи:
```bash
# Посмотреть логи
cat logs/autocommiter.log
tail -f logs/autocommiter.log  # следить в реальном времени
```

### История коммитов:
```bash
# Последние 10 коммитов
python src/main.py history --limit 10
```

## ⚠️ Важные ограничения

1. **Максимум 100 коммитов за запуск** (настройка `security.max_commits_per_run`)
2. **Автоотключение через 24 часа** (настройка `security.shutdown_after_hours`)
3. **Требуется подтверждение** при каждом запуске
4. **Работает только в указанные часы** (по умолчанию 9:00-21:00)

## 🔒 Безопасность и этика

### Каждый запуск показывает предупреждение:
```
⚠️ ETHICAL WARNING ⚠️
This tool is for EDUCATIONAL PURPOSES ONLY.
Press Enter to continue or Ctrl+C to exit...
```

### Допустимое использование:
- Обучение Git автоматизации
- Тестирование CI/CD систем
- Демонстрационные проекты

### Недопустимое использование:
- Обман преподавателей
- Фальсификация работы
- Нарушение академической честности

## 🚨 Экстренная остановка

### Если автокоммиттер вышел из-под контроля:
1. **Закройте терминал** с запущенным скриптом
2. **Удалите автозапуск**: `python src/main.py uninstall-service`
3. **Удалите задание из Планировщика задач Windows** (если установлено)
4. **Удалите из автозагрузки реестра** (если установлено)

## 📞 Поддержка

### Шаги отладки:
1. Проверьте конфигурацию: `python src/main.py config show`
2. Проверьте репозиторий: `python src/main.py status`
3. Проверьте логи: `cat logs/autocommiter.log`
4. Запустите тест: `python src/main.py test-commit`

### Типичные проблемы:
| Проблема | Решение |
|----------|---------|
| Пустой репозиторий | Настройте `repository.local_path` |
| Нет Git | Установите Git и добавьте в PATH |
| Нет прав | Запустите от имени администратора |
| Ошибка YAML | Проверьте синтаксис в `user_config.yaml` |

## 🎓 Образовательные сценарии

### 1. Изучение Git графиков активности
```bash
# Настройте 2-3 коммита в день
python src/main.py config set commit_settings.min_commits_per_day 2
python src/main.py config set commit_settings.max_commits_per_day 3
python src/main.py start --background
# Наблюдайте за графиком на GitHub
```

### 2. Тестирование Git хуков
```bash
# Создайте .git/hooks/pre-commit
# Запустите автокоммиттер
# Проверьте работу хуков
```

### 3. Демо CI/CD пайплайнов
```bash
# Настройте автокоммиты
# Настройте GitHub Actions
# Наблюдайте автоматические тесты
```

---

## 📝 Чеклист перед запуском

- [ ] Git установлен и в PATH
- [ ] Python 3.8+ установлен
- [ ] Зависимости установлены (`requirements.txt`)
- [ ] Репозиторий настроен (`repository.local_path`)
- [ ] Понимаете этические последствия
- [ ] Используете на своем репозитории

## 🏁 Быстрая команда для всего

```bash
# Одна команда для установки и запуска теста
git clone <repo> && cd student-autocommiter && pip install -r requirements.txt && python src/main.py init && python src/main.py test-commit
```

---

**Запомните**: Этот инструмент создан для обучения, а не для обмана. Используйте ответственно! 🎓